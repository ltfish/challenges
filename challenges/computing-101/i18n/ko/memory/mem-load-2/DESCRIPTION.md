아주 조금만 더 연습이 필요해 보이네요.
이 레벨에서는 비밀 값을 `133700`이 아니라 `123400`에 두었습니다:

```text
  Address │ Contents
+────────────────────+
│ 123400  │ ???      │
+────────────────────+
```

그것을 `rdi`로 읽어 들이고 그 값을 종료 코드로 삼아 `exit`하세요!
