앞 레벨에서는 GDB가 알아서 종료해 주었습니다.
이제 여러분 차례입니다!

GDB에서 작업을 마쳤으면 `quit` 명령(또는 그냥 `q`)으로 빠져나옵니다:

```gdb
(gdb) quit
```

이 레벨에서도 분석은 저희가 대신 해 드립니다.
여러분이 할 일은 GDB를 실행하고, 마법이 일어나게 두었다가, `quit`을 입력해 빠져나오는 것뿐입니다.
