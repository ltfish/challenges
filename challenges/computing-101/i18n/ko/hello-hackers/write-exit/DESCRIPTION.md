자, 앞의 풀이는 출력은 했지만 그 뒤에 죽어버렸습니다.
이 레벨에서는 출력을 하고 _죽지 않게_ 만듭니다!

`write` 시스템 콜을 호출한 다음 `exit` 시스템 콜을 호출해 프로그램을 깔끔하게 종료하면 됩니다.
시스템 콜 두 개는 어떻게 호출할까요?
명령어 두 개를 실행하는 것과 똑같습니다!
먼저 필요한 레지스터를 준비해 `write`를 호출하고, 그다음 필요한 레지스터를 준비해 `exit`를 호출합니다!

앞의 풀이에는 명령어가 5개 있었습니다(스택에서 첫 인자의 주소 불러오기, `rdi` 설정, `rdx` 설정, `rax` 설정, `syscall`).
이번에는 그 5개에 더해 `exit`를 위한 3개가 필요합니다(`rdi`에 종료 코드 설정, `rax`에 시스템 콜 번호 `60` 설정, `syscall`).
이 레벨에서는 종료 코드 `42`로 끝내 봅시다!
