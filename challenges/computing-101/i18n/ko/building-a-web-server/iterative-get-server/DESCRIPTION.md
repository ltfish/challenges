앞에서 여러분의 서버는 GET 요청 하나만 처리하고 종료했습니다.
이제 여러 GET 요청을 차례로 처리할 수 있도록 고칩니다.
accept-read-write-close 순서를 반복문으로 감싸면 됩니다.
클라이언트가 접속할 때마다 서버는 연결을 받아들이고, GET 요청을 처리하고, 클라이언트 세션을 깔끔하게 닫으면서도 다음 요청을 위해 계속 살아 있습니다.
이런 순차적 방식은 오래 살아 있는 서버를 만드는 데 필수적입니다.
