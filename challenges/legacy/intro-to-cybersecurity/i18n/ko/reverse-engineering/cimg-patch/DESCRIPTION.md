여러 지시어를 지원하면 꽤 고급스러운 cIMG 기능이 가능해집니다.
예를 들어 앞에서는 필요한 이미지를 만들 때 모든 픽셀을 하나하나 지정했습니다.
하지만 이 레벨에서 추가된 고급 기능 덕분에 더는 그럴 필요가 없습니다!
그래서 올바른 이미지를 만드는 데 바이트를 너무 많이 쓰면 챌린지가 플래그를 주지 않습니다.
행운을 빕니다.

----
**접근 방법 제안:**
이 레벨에서는 한 파일 안에 여러 지시어가 담긴 cimage를 만들어야 합니다.
도움이 되었으면 하는 제안들입니다:

- 처음 몇 번의 시도는 대체로 오류 메시지로 끝날 것입니다.
  **그 오류 메시지를 어떻게 고칠지 그냥 찍지 마세요!**
  대신 그래픽 리버싱 도구와 디버거(gdb)를 함께 써서 어떤 검사가 실패하는지 실제로 이해하고, 그 검사를 통과하도록 입력을 고치세요.
- cimage를 손으로 쓰면 실수하기 아주 쉽습니다.
  cimage를 만들어 주는 Python 스크립트를 작성하는 것을 고려하세요.
  그 스크립트는 파서 라이브러리를 어느 정도 흉내 내어, cimage 헤더를 만드는 함수와 지원하려는 지시어마다 함수를 갖추면 좋습니다.
  이렇게 하면 이 레벨과 앞으로의 레벨에서 삶이 훨씬 편해집니다.
