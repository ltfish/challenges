`tr`은 문자를 아무것도 아닌 것으로 바꿀 수도 있습니다(즉, _삭제_ 합니다).
`-d` 플래그와 지울 문자를 인자로 주면 됩니다:

```console
hacker@dojo:~$ echo PAWN | tr -d A
PWN
hacker@dojo:~$
```

꽤 간단하죠!
이제 직접 해보세요.
`/challenge/run`의 출력에서 플래그 문자 사이에 가짜 문자(구체적으로는 `^`와 `%`)를 끼워 넣겠습니다.
`tr -d`로 그것들을 지우세요!
