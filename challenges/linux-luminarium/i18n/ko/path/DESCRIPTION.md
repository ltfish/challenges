지금까지 여러분은 여러 방식으로 명령어를 실행해 왔습니다:

- 절대 경로로 (예: `/challenge/run`).
- 상대 경로로 (예: `./run`).
- 명령어 이름만으로 (예: `ls`).

앞의 두 경우, 절대 경로와 상대 경로는 간단합니다: `run` 파일은 `/challenge` 디렉터리에 있고, 두 방식 모두 그 파일을 가리킵니다(물론 상대 경로는 현재 작업 디렉터리가 `/challenge`일 때의 이야기입니다).
그런데 마지막 경우는 어떨까요?
`ls` 프로그램은 대체 어디에 있을까요?
셸은 그것을 거기서 찾아야 한다는 걸 어떻게 알까요?

이 모듈에서는 그 장막을 걷어내고 이 질문에 답합니다!
끝까지 함께해 주세요.
