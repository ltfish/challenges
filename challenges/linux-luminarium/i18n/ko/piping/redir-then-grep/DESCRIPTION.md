명령을 실행하는 법, 그 출력을 리다이렉트하는 법(예: `>`), 그리고 만들어진 파일에서 검색하는 법(예: `grep`)을 알고 있습니다.
이제 이것들을 합쳐 봅시다!

더 복잡한 레벨을 준비하는 차원에서, 다음을 해보세요:

1. `/challenge/run`의 출력을 `/tmp/data.txt`로 리다이렉트하세요.
2. 그러면 `/tmp/data.txt`에 십만 줄의 텍스트가 생기고, 그중 한 줄이 플래그입니다.
3. 거기서 `grep`으로 플래그를 찾으세요!
